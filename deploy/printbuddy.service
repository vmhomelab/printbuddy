# Printbuddy Systemd Service Template
#
# INSTALLATION:
# 1. Copy this file to /etc/systemd/system/printbuddy.service
# 2. Replace placeholders:
#    - INSTALL_PATH: Where Printbuddy is installed (e.g., /opt/printbuddy)
#    - SERVICE_USER: User to run as (e.g., printbuddy)
#    - DATA_DIR: Data directory (e.g., /opt/printbuddy/data)
#    - LOG_DIR: Log directory (e.g., /opt/printbuddy/logs)
# 3. Run: sudo systemctl daemon-reload
# 4. Run: sudo systemctl enable printbuddy
# 5. Run: sudo systemctl start printbuddy
#
# Or use the install script: ./install/install.sh
#

[Unit]
Description=Printbuddy - 3D Printer Management
Documentation=https://github.com/vmhomelab/Printbuddy
After=network.target

[Service]
Type=simple
User=SERVICE_USER
Group=SERVICE_USER
WorkingDirectory=INSTALL_PATH

# Environment file (optional - created by install script)
EnvironmentFile=-INSTALL_PATH/.env

# Use virtual environment
Environment="PATH=INSTALL_PATH/venv/bin:/usr/local/bin:/usr/bin:/bin"

# Server configuration
ExecStart=INSTALL_PATH/venv/bin/uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000}

# Restart policy
Restart=on-failure
RestartSec=5

# Graceful shutdown
TimeoutStopSec=10

# Kill zombie ffmpeg processes (timelapse processing)
ExecStartPre=-/usr/bin/pkill -9 -f "ffmpeg.*printbuddy"
ExecStopPost=-/usr/bin/pkill -9 -f "ffmpeg.*printbuddy"

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=printbuddy

# Security hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=DATA_DIR LOG_DIR INSTALL_PATH

[Install]
WantedBy=multi-user.target
